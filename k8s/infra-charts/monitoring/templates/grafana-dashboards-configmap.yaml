apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-dashboards
  labels:
    app: grafana
    release: {{ .Release.Name }}
data:
  team-service-dashboard.json: |
{{ .Files.Get "dashboards/team-service-dashboard.json" | indent 4 }}

  bracket-service-dashboard.json: |
{{ .Files.Get "dashboards/bracket-service-dashboard.json" | indent 4 }}
